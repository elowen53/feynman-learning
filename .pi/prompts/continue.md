---
description: Continue from the exact saved learning node
argument-hint: "<project>"
---

Use the `feynman-coach` skill.

Continue project: `$ARGUMENTS`.

Load the saved progress and resume from `progress.json.next_action`. If `sessions/` contains a latest session note, use it to recover the exact continuation sentence. Do not restart the topic unless the saved state requires rebuilding.
