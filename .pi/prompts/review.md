---
description: Run user-triggered review for a Feynman project
argument-hint: "<project>"
---

Use the `feynman-coach` skill.

Run review for project: `$ARGUMENTS`.

Only review because the user explicitly invoked this command. Choose review targets from `reviews.json` and `progress.json`, prioritizing misconceptions, low scores, stale concepts, and prerequisites. Use the Feynman loop: ask the learner to explain first, then correct and score.
